import { audit, calculateStyle, ensureSchema, json, nowIso, requireAdmin, toNumber, uid } from '../../../_utils.js';
export async function onRequestPost(context){const unauthorized=await requireAdmin(context);if(unauthorized)return unauthorized;const db=context.env.DB;await ensureSchema(db);const b=await context.request.json();const session=await db.prepare(`SELECT * FROM scan_sessions WHERE id=?`).bind(b.session_id).first();const style=await db.prepare(`SELECT * FROM styles WHERE id=?`).bind(b.style_id).first();if(!session||!style)return json({error:'Session or style not found.'},404);const components=await db.prepare(`SELECT * FROM diamond_components WHERE style_id=? ORDER BY sort_order`).bind(style.id).all();const calc=await calculateStyle(db,style,components.results||[]);const markup=toNumber(b.markup_pct,session.default_markup);const suggested=calc.totals.total_import_cost*(1+markup/100);const existing=await db.prepare(`SELECT * FROM scan_items WHERE session_id=? AND barcode=?`).bind(session.id,b.barcode||'').first();if(existing&&!b.allow_duplicate)return json({duplicate:true,item:existing},409);if(existing){await db.prepare(`UPDATE scan_items SET quantity=quantity+1,updated_at=? WHERE id=?`).bind(nowIso(),existing.id).run();await audit(db,context.data.user,'DUPLICATE_SCAN_ADDED','scan_item',existing.id,existing,{quantity:Number(existing.quantity)+1});return json({ok:true,item:await db.prepare(`SELECT * FROM scan_items WHERE id=?`).bind(existing.id).first()});}const id=uid('scanitem'),now=nowIso();await db.prepare(`INSERT INTO scan_items (id,session_id,barcode,source_style_no,style_id,quantity,cost_snapshot,cttw_snapshot,markup_pct,final_price,resolution_status,created_at,updated_at) VALUES (?,?,?,?,?,1,?,?,?,?,?,?,?)`).bind(id,session.id,b.barcode||'',b.source_style_no||'',style.id,calc.totals.total_import_cost,calc.totals.cttw,markup,toNumber(b.final_price,suggested),b.resolution_status||'resolved',now,now).run();await db.prepare(`UPDATE scan_sessions SET updated_at=? WHERE id=?`).bind(now,session.id).run();const saved=await db.prepare(`SELECT * FROM scan_items WHERE id=?`).bind(id).first();await audit(db,context.data.user,'PIECE_SCANNED','scan_item',id,null,saved);return json({ok:true,item:saved});}
export async function onRequestPut(context){const unauthorized=await requireAdmin(context);if(unauthorized)return unauthorized;const db=context.env.DB;await ensureSchema(db);const b=await context.request.json();const before=await db.prepare(`SELECT * FROM scan_items WHERE id=?`).bind(b.id).first();if(!before)return json({error:'Scan item not found.'},404);await db.prepare(`UPDATE scan_items SET quantity=?,markup_pct=?,final_price=?,updated_at=? WHERE id=?`).bind(Math.max(1,Number(b.quantity)||1),toNumber(b.markup_pct),toNumber(b.final_price),nowIso(),b.id).run();const saved=await db.prepare(`SELECT * FROM scan_items WHERE id=?`).bind(b.id).first();await audit(db,context.data.user,'SCAN_ITEM_UPDATED','scan_item',b.id,before,saved);return json({ok:true,item:saved});}
export async function onRequestDelete(context){const unauthorized=await requireAdmin(context);if(unauthorized)return unauthorized;const db=context.env.DB;await ensureSchema(db);const id=new URL(context.request.url).searchParams.get('id');const before=await db.prepare(`SELECT * FROM scan_items WHERE id=?`).bind(id).first();if(!before)return json({error:'Scan item not found.'},404);await db.prepare(`DELETE FROM scan_items WHERE id=?`).bind(id).run();await audit(db,context.data.user,'SCAN_ITEM_DELETED','scan_item',id,before);return json({ok:true});}
